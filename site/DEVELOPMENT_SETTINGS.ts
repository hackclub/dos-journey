/**
 * Settings for when contributing to the site.
 */
const DevelopmentSettings = {
  ANIMATE_LAYERS: true, // Animate the layers on adventure stages in response to mouse movement? Default true.
}

export default DevelopmentSettings;